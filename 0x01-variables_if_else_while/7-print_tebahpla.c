#include <stdio.h>

/**
* main - Entery point
*
* Return: Always 0 (Success)
*/

int main(void)
{

char alpha;

for (alpha = 'z'; alpha >= 'a'; num--)
{
putchar(alpha);
}
putchar('\n');

return (0);
}
