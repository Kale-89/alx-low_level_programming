#include <stdio.h>
#include <stdlib.h>

/**
* main - Entery point
*
* Return: Always 0 (Success)
*/

int main(void)
{
char = alpha;
for (alpha = 'a'; alpha <= 'z'; alpha++)
{
putchar (alpha);
}

putchar(alpha);

return (0);
}
